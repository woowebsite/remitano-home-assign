export type VideoData = {
  id: string;
  title: string;
  description: string;
  youtubeId: string;
  createBy: any;
};

export type LoginRegisterFormData = {
    email: string;
    password: string;
}